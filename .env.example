NODE_ENV=development
API_PORT=14000
API_HOST=0.0.0.0
DATABASE_URL=postgresql://mmsc:mmsc_dev_password@localhost:15432/mmsc
LOG_LEVEL=info
CORS_ORIGIN=http://localhost:15173,http://localhost:15174
MEDIA_STORAGE_PATH=./data/uploads
VITE_API_URL=http://localhost:14000/api/v1
SESSION_TTL_HOURS=12
SESSION_COOKIE_SECURE=false
TRUST_PROXY=false
# Optional one-time bootstrap used by `pnpm db:seed`; set both values locally.
# BOOTSTRAP_ADMIN_EMAIL=admin@example.edu.ph
# BOOTSTRAP_ADMIN_USERNAME=superadmin
# BOOTSTRAP_ADMIN_PASSWORD=use-a-unique-password-of-at-least-12-characters
# BOOTSTRAP_ADMIN_NAME=MMSC Super Administrator
# Local/demo only: both values are required by `pnpm demo:reset`.
# MMSC_DEMO_RESET=RESET_LOCAL_MMSC_DEMO
# MMSC_DEMO_PASSWORD=choose-a-local-demo-password-of-at-least-12-characters
