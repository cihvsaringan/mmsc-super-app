import{z}from'zod';const uuid=z.string().uuid(),text=z.string().trim().max(1000).nullable().optional(),timestamp=z.iso.datetime({offset:true}).nullable().optional();
export const attendanceStatus=z.enum(['present','late','absent','half_day','on_leave','holiday','rest_day']);export const attendanceSource=z.enum(['manual','qr_terminal','rfid_terminal','administrative_correction','imported']);
export const attendanceCreateSchema=z.object({employeeId:uuid,attendanceDate:z.iso.date(),timeIn:timestamp,timeOut:timestamp,status:attendanceStatus,source:attendanceSource.default('manual'),externalEventId:z.string().trim().max(200).nullable().optional(),minutesLate:z.coerce.number().int().min(0).default(0),notes:text}).strict().refine(v=>!v.timeIn||!v.timeOut||v.timeOut>=v.timeIn,{path:['timeOut'],message:'Time out must not precede time in'});
export const correctionSchema=z.object({requestedTimeIn:timestamp,requestedTimeOut:timestamp,requestedStatus:attendanceStatus,reason:z.string().trim().min(1).max(1000)}).strict();
export const reviewSchema=z.object({decision:z.enum(['approved','rejected']),reviewNotes:text,minutesLate:z.coerce.number().int().min(0).optional()}).strict();
export const adjustmentSchema=z.object({timeIn:timestamp,timeOut:timestamp,status:attendanceStatus,minutesLate:z.coerce.number().int().min(0),reason:z.string().trim().min(1).max(1000),notes:text}).strict();
