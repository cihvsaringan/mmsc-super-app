import{z}from'zod';const uuid=z.uuid(),nullableUuid=uuid.nullable().optional();export const issueCategory=z.enum(['hardware','software','network','peripheral','power','account_access','environment','other']);export const issuePriority=z.enum(['low','medium','high','critical']);export const issueStatus=z.enum(['reported','acknowledged','in_progress','resolved','closed','cancelled']);export const maintenanceType=z.enum(['preventive','corrective','inspection','repair','replacement','software_service','network_service','cleaning','other']);
export const issueCreateSchema=z.object({laboratoryId:uuid,workstationId:nullableUuid,category:issueCategory,priority:issuePriority,title:z.string().trim().min(3).max(200),description:z.string().trim().min(10).max(5000),workstationStatus:z.enum(['maintenance','offline']).optional()}).strict();
export const issueQuerySchema=z.object({search:z.string().trim().max(100).optional(),laboratoryId:uuid.optional(),workstationId:uuid.optional(),category:issueCategory.optional(),priority:issuePriority.optional(),status:issueStatus.optional(),assigneeId:uuid.optional(),limit:z.coerce.number().int().min(1).max(100).default(25),offset:z.coerce.number().int().min(0).default(0)}).strict();
export const assignmentSchema=z.object({employeeId:uuid.nullable()}).strict();export const transitionSchema=z.object({action:z.enum(['acknowledge','start_work','resolve','close','cancel']),notes:z.string().trim().max(5000).optional()}).strict();
export const maintenanceCreateSchema=z.object({laboratoryId:uuid,workstationId:nullableUuid,issueId:nullableUuid,maintenanceType,description:z.string().trim().min(5).max(5000),performedByEmployeeId:nullableUuid,performedAt:z.iso.datetime({offset:true}),cost:z.string().regex(/^\d{1,10}(\.\d{1,2})?$/).nullable().optional(),partsOrMaterials:z.string().trim().max(5000).nullable().optional(),nextRecommendedMaintenanceAt:z.iso.datetime({offset:true}).nullable().optional(),notes:z.string().trim().max(5000).nullable().optional(),resolveLinkedIssue:z.boolean().optional(),resolutionNotes:z.string().trim().max(5000).optional()}).strict().superRefine((v,c)=>{if(v.resolveLinkedIssue&&!v.issueId)c.addIssue({code:'custom',path:['issueId'],message:'Select a linked issue'});if(v.resolveLinkedIssue&&(!v.resolutionNotes||v.resolutionNotes.length<5))c.addIssue({code:'custom',path:['resolutionNotes'],message:'Resolution notes are required'});});
export const maintenanceQuerySchema=z.object({search:z.string().trim().max(100).optional(),laboratoryId:uuid.optional(),workstationId:uuid.optional(),maintenanceType:maintenanceType.optional(),performedByEmployeeId:uuid.optional(),from:z.iso.date().optional(),to:z.iso.date().optional(),limit:z.coerce.number().int().min(1).max(100).default(25),offset:z.coerce.number().int().min(0).default(0)}).strict();
