import{describe,expect,it}from'vitest';import{scheduleCreateSchema}from'./schedule-schemas.js';import{scheduleOccurrences,schedulesOverlap}from'./schedule-repository.js';
const id='11111111-1111-4111-8111-111111111111';const one=(start='10:00',end='11:00',date='2026-09-07')=>scheduleCreateSchema.parse({laboratoryId:id,scheduleType:'event',title:'ICT Event',recurrenceType:'one_time',scheduleDate:date,startTime:start,endTime:end});const weekly=(day=1,startDate='2026-09-01',endDate='2026-12-15',start='10:00',end='11:00')=>scheduleCreateSchema.parse({laboratoryId:id,scheduleType:'class',schoolYearId:id,teachingAssignmentId:id,recurrenceType:'weekly',dayOfWeek:day,recurrenceStartDate:startDate,recurrenceEndDate:endDate,startTime:start,endTime:end});
describe('Computer Laboratory scheduling',()=>{it('rejects invalid time ranges',()=>{expect(()=>one('10:00','10:00')).toThrow();expect(()=>one('11:00','10:00')).toThrow()});it('uses open interval overlap and allows adjacent schedules',()=>{const current=one();for(const next of[one('10:30','11:30'),one('09:30','10:30'),one('10:00','11:00')])expect(schedulesOverlap(current,next)).toBe(true);for(const next of[one('09:00','10:00'),one('11:00','12:00')])expect(schedulesOverlap(current,next)).toBe(false)});it('detects actual weekly occurrences only',()=>{expect(schedulesOverlap(weekly(),weekly(1,'2026-10-01','2026-11-30','10:30','11:30'))).toBe(true);expect(schedulesOverlap(weekly(),weekly(2,'2026-10-01','2026-11-30','10:30','11:30'))).toBe(false);expect(schedulesOverlap(weekly(1,'2026-09-01','2026-09-30'),weekly(1,'2026-10-01','2026-11-30'))).toBe(false);expect(scheduleOccurrences(weekly()).length).toBeGreaterThan(10)});it('requires authoritative class references and bounded recurrence',()=>{expect(()=>scheduleCreateSchema.parse({...weekly(),teachingAssignmentId:null})).toThrow();expect(()=>weekly(1,'2026-01-01','2027-02-01')).toThrow()})});
