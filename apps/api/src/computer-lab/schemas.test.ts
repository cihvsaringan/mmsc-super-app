import{describe,expect,it}from'vitest';import{labCreateSchema,workstationCreateSchema}from'./schemas.js';
const lab={name:'Main Computer Lab',code:'comlab-01',campusId:'11111111-1111-4111-8111-111111111111',locationOrRoom:'Room 201',capacity:40,operationalStatus:'active',walkInEnabled:true,walkInApprovalRequired:true};
describe('computer laboratory validation',()=>{it('normalizes lab codes and validates capacity/status',()=>{expect(labCreateSchema.parse(lab).code).toBe('COMLAB-01');expect(()=>labCreateSchema.parse({...lab,capacity:0})).toThrow();expect(()=>labCreateSchema.parse({...lab,operationalStatus:'open'})).toThrow()});it('validates workstation network and dates without exposing in_use',()=>{const base={labId:lab.campusId,workstationCode:'PC-01',operationalStatus:'available'};expect(workstationCreateSchema.parse({...base,ipAddress:'192.168.1.10',macAddress:'AA:BB:CC:DD:EE:FF'}).workstationCode).toBe('PC-01');expect(()=>workstationCreateSchema.parse({...base,ipAddress:'999.1.1.1'})).toThrow();expect(()=>workstationCreateSchema.parse({...base,macAddress:'invalid'})).toThrow();expect(()=>workstationCreateSchema.parse({...base,operationalStatus:'in_use'})).toThrow();expect(()=>workstationCreateSchema.parse({...base,purchaseDate:'2026-02-01',warrantyExpirationDate:'2026-01-01'})).toThrow()})});
