import{z}from'zod';export const gradeRowsSchema=z.object({version:z.number().int().positive(),rows:z.array(z.object({enrollmentId:z.string().uuid(),rawScore:z.number().min(0).nullable(),finalGrade:z.number().min(0).max(100).nullable(),remarks:z.string().trim().max(1000).nullable()})).max(200)}).strict();export const transitionSchema=z.object({version:z.number().int().positive(),reason:z.string().trim().min(3).max(1000).nullable().default(null)}).strict();export const gradebookCreateSchema=z.object({teachingAssignmentId:z.string().uuid(),gradingPeriodId:z.string().uuid()}).strict();
