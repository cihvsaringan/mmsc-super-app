import{z}from'zod';const uuid=z.string().uuid(),timestamp=z.iso.datetime({offset:true}).nullable().optional(),notes=z.string().trim().max(1000).nullable().optional();export const studentAttendanceStatus=z.enum(['present','late','absent','half_day','excused','on_leave','holiday','rest_day']);export const studentAttendanceSource=z.enum(['manual','qr_terminal','rfid_terminal','administrative_correction','imported']);
export const studentAttendanceCreateSchema=z.object({enrollmentId:uuid,attendanceDate:z.iso.date(),attendanceScope:z.enum(['campus','class']).default('campus'),teachingAssignmentId:uuid.nullable().optional(),timeIn:timestamp,timeOut:timestamp,status:studentAttendanceStatus,source:studentAttendanceSource.default('manual'),externalEventId:z.string().trim().max(200).nullable().optional(),minutesLate:z.coerce.number().int().min(0).default(0),notes}).strict().superRefine((v,c)=>{if(v.timeIn&&v.timeOut&&v.timeOut<v.timeIn)c.addIssue({code:'custom',path:['timeOut'],message:'Time out must not precede time in'});if(v.attendanceScope==='class'&&!v.teachingAssignmentId)c.addIssue({code:'custom',path:['teachingAssignmentId'],message:'Class attendance requires a teaching assignment'});if(v.attendanceScope==='campus'&&v.teachingAssignmentId)c.addIssue({code:'custom',path:['teachingAssignmentId'],message:'Campus attendance cannot reference a teaching assignment'});});
export const studentAttendanceAdjustmentSchema=z.object({timeIn:timestamp,timeOut:timestamp,status:studentAttendanceStatus,minutesLate:z.coerce.number().int().min(0),reason:z.string().trim().min(1).max(1000),notes}).strict();
