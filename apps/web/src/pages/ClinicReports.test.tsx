import{cleanup,fireEvent,render,screen}from'@testing-library/react';import{afterEach,describe,expect,it,vi}from'vitest';vi.mock('../auth/AuthContext',()=>({useAuth:()=>({has:(p:string)=>p==='report.export'})}));import{ClinicReports}from'./ClinicReports';describe('Clinic reports',()=>{afterEach(()=>{cleanup();vi.unstubAllGlobals()});it('renders filtered aggregate operations results and export action',async()=>{vi.stubGlobal('fetch',vi.fn().mockResolvedValue(new Response(JSON.stringify({rows:[{date:'2026-08-27',grade:'Grade 6',visits:4,sentHome:1}]}),{status:200,headers:{'Content-Type':'application/json'}})));render(<ClinicReports aggregateOnly/>);expect(screen.getByText(/internal clinical notes are excluded/i)).toBeInTheDocument();fireEvent.click(screen.getByRole('button',{name:/run report/i}));expect(await screen.findByText('Grade 6')).toBeInTheDocument();expect(screen.getByRole('link',{name:/export csv/i})).toBeInTheDocument()});});
