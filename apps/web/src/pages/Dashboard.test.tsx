import{fireEvent,render,screen}from'@testing-library/react';import{MemoryRouter}from'react-router-dom';import{beforeEach,describe,expect,it,vi}from'vitest';import{Dashboard}from'./Dashboard';
const data={asOf:'2026-08-23T08:00:00Z',schoolYear:{id:'year',name:'2026–2027',startsOn:'2026-06-01',endsOn:'2027-03-31'},access:{students:true,studentAttendance:true,employees:true,teachers:true,admissions:true,enrollments:true,attendanceExceptions:true,gradeReview:true},students:{byGrade:[{gradeLevelId:'g1',gradeLevel:'Grade 1',sequence:1,students:42}],bySection:[{gradeLevelId:'g1',gradeLevel:'Grade 1',sequence:1,sectionId:'s1',section:'Faith',students:42}]},studentAttendance:[{gradeLevelId:'g1',gradeLevel:'Grade 1',sequence:1,status:'present',count:40}],employeeAttendance:[{departmentId:'d1',department:'Academic',employeeTypeId:'t1',employeeType:'Regular',employeeId:'e1',teacher:true,status:'present'}],admissions:[{status:'under_review',count:3}],enrollments:[{status:'enrolled',count:42}],attendanceExceptions:[{status:'open',count:1}],gradeReview:[{status:'submitted',count:2}]};
beforeEach(()=>vi.stubGlobal('fetch',vi.fn().mockResolvedValue({ok:true,status:200,json:async()=>data})));
describe('Dashboard operational tabs',()=>{it('shows primary areas and student secondary views without simultaneous datasets',async()=>{render(<MemoryRouter><Dashboard/></MemoryRouter>);expect(await screen.findByRole('tab',{name:'Students'})).toHaveAttribute('aria-selected','true');expect(screen.getByRole('tab',{name:'Employees'})).toBeInTheDocument();expect(screen.getByRole('tab',{name:'Teachers'})).toBeInTheDocument();expect(screen.getByRole('tab',{name:'More Operations'})).toBeInTheDocument();expect(screen.getByRole('tab',{name:'Enrollment by Grade'})).toHaveAttribute('aria-selected','true');fireEvent.click(screen.getByRole('tab',{name:'Students by Section'}));expect(screen.getByText('How are students distributed across sections?')).toBeInTheDocument();expect(screen.queryByText('How are currently enrolled students distributed?')).not.toBeInTheDocument()})});
