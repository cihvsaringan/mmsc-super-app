import{KeyRound}from'lucide-react';import{useState,type FormEvent}from'react';import{useAuth}from'../auth/AuthContext';import{ApiError}from'../lib/api';
export function RequiredPasswordChange(){const{user,changePassword,logout}=useAuth();const[error,setError]=useState('');const[saving,setSaving]=useState(false);const submit=async(event:FormEvent<HTMLFormElement>)=>{event.preventDefault();const form=new FormData(event.currentTarget);const next=String(form.get('newPassword'));if(next!==form.get('confirmPassword')){setError('New passwords do not match.');return;}setSaving(true);setError('');try{await changePassword(String(form.get('currentPassword')),next);}catch(cause){setError(cause instanceof ApiError?cause.message:'Unable to change password');}finally{setSaving(false);}};return <main className="password-gate"><form className="login-card" onSubmit={submit}><img src="/mmsc-logo.jpg" alt="MMSC crest"/><span className="login-icon"><KeyRound/></span><h1>Choose a private password</h1><p>Hello, {user?.displayName}. Your temporary or administrator-issued password must be replaced before you can continue.</p>{error&&<div className="form-error" role="alert">{error}</div>}<label>Current password<input name="currentPassword" type="password" autoComplete="current-password" required/></label><label>New password<input name="newPassword" type="password" autoComplete="new-password" minLength={12} required/></label><label>Confirm new password<input name="confirmPassword" type="password" autoComplete="new-password" minLength={12} required/></label><small>Use 12 or more characters with uppercase, lowercase, number, and symbol characters.</small><button className="button" disabled={saving}>{saving?'Changing…':'Change password and continue'}</button><button type="button" className="text-button" disabled={saving} onClick={()=>void logout()}>Sign out</button></form></main>}
